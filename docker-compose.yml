version: "3.9"

services:
  backend:
    build:
      context: ./backend
    container_name: app_backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend:/app
    restart: unless-stopped

